<div class="dashboard-header">
  <h1 class="dashboard-title" style="font-size: 2.5rem;">Jurnal Tulisan</h1>
  <p class="dashboard-subtitle" style="font-size: 1.1rem;">Kumpulan pemikiran dan catatan teknis selama proses pengembangan.</p>
</div>

<% if @posts.any? %>
  <div class="posts-grid">
    <%= render partial: 'post_card', collection: @posts, as: :post %>
  </div>
<% else %>
  <div class="glass-card text-center" style="padding: 80px 40px; border-style: dashed;">
    <p style="color: var(--text-secondary); margin-bottom: 24px;">Belum ada tulisan yang dipublikasikan.</p>
    <% if admin_signed_in? %>
      <%= link_to new_post_path, class: "btn-ofelos btn-primary-ofelos" do %>
        Tulis Post Pertama
      <% end %>
    <% end %>
  </div>
<% end %>

