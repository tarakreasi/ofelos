<div style="max-width: 800px; margin: 0 auto;">
  <div style="margin-bottom: 40px;">
    <%= link_to post_path(@post), style: "color: var(--text-muted); text-decoration: none; display: flex; align-items: center; gap: 8px; font-size: 0.9rem; margin-bottom: 16px;" do %>
      ← Batal Edit
    <% end %>
    <h1 style="font-size: 2.5rem; font-weight: 800; color: white;">Edit Tulisan</h1>
    <p style="color: var(--text-secondary);">Memperbaiki atau memperbarui catatan sebelumnya.</p>
  </div>

  <div class="glass-card">
    <%= render 'form' %>
  </div>
</div>
